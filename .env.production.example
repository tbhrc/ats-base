# Copy to .env.production on the VPS (chmod 0640, never commit the real file).
# Referenced by docker-compose.production.yml.

DATABASE_NAME=cats
DATABASE_USER=ats
DATABASE_PASS=changeme-generate-a-real-secret
MYSQL_ROOT_PASSWORD=changeme-generate-a-real-secret
SSL_ENABLED=true
APP_PORT=3011
